# x86_64 jniLibs

Place these files here:
- `libproot.so`  — PRoot binary compiled for x86_64 (emulator)
- `libbash.so`   — Bash binary compiled for x86_64 (emulator)

See parent README.md for instructions.
