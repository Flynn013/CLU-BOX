# arm64-v8a jniLibs

Place these files here:
- `libproot.so`  — PRoot binary compiled for AArch64 (arm64-v8a)
- `libbash.so`   — Bash binary compiled for AArch64 (arm64-v8a)

See parent README.md for instructions.
